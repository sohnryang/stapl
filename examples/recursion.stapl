def fib(n: int): int {
  if n <= 1 {
    return n
  } else {
    return fib(n - 1) + fib(n - 2)
  }
}

def factorial(n: int): int {
  if n == 0 {
    return 1
  } else {
    return n * factorial(n - 1)
  }
}
